<?xml version="1.0" encoding="utf-8"?>
<!-- Только отладочная сборка: локальный стенд эмулятора (хост = 10.0.2.2) по HTTP.
     В релизной сборке этого файла нет — там весь трафик только HTTPS. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
